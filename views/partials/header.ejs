<header ng-controller="HeaderController">
  <form class="search" ng-submit="search(query)">
    <input ng-model="query" placeholder="Search users, lists, or posts..."/>
    <button><span class="icon ion-ios-search-strong"></span></button>

    <div class="suggestions" ng-if="suggestions" ng-cloak>
      <div class="suggestion" ng-repeat="item in suggestions">
        {{item.text}}
      </div>
    </div>
  </form>

  <% if (user && user._id) { %>
    <div class="align">
      <a href="/logout">Logout</a>
      <% if (user.image) { %>
        <img src="<%= user.image %>" alt="<%= user.displayName %>" send-to="/user/<%= user._id %>"/>
      <% } %>
    </div>
  <% } else { %>
    <a href="/auth/facebook">Sign Up / Login</a>
  <% } %>
</header>
